## 1. 代数证明

已知一元线性回归模型：y_i = β0 + β1・x_i + ε_i，残差定义为 e_i = y_i - ŷ_i = y_i - β̂0 - β̂1・x_i。最小二乘估计使残差平方和 Σe_i² 最小，对 β̂0、β̂1 分别求偏导并令其为零，得到正规方程。

**证明 Σe_i = 0：**

对 β̂0 求偏导并令其为零：

∂(Σe_i²)/∂β̂0 = -2·Σ(y_i - β̂0 - β̂1·x_i) = 0

即：

Σ(y_i - β̂0 - β̂1·x_i) = 0

代入 e_i = y_i - β̂0 - β̂1・x_i，得：

Σe_i = 0

**证明 Σx_i・e_i = 0：**

对 β̂1 求偏导并令其为零：

∂(Σe_i²)/∂β̂1 = -2·Σx_i·(y_i - β̂0 - β̂1·x_i) = 0

即：

Σx_i·(y_i - β̂0 - β̂1·x_i) = 0

代入 e_i 的定义，得：

Σx_i·e_i = 0

**结论：** 第一个等式（残差和为零）要求模型包含截距项；第二个等式（残差与自变量乘积和为零）对带截距的一元线性回归恒成立。两者共同说明残差向量与设计矩阵的列空间正交。

------

## 2. 理解思考题

R² 的定义：R² = 1 - RSS / TSS

调整后 R² 的定义：R²_adj = 1 - (RSS / (n - p - 1)) / (TSS / (n - 1))

其中 n 为样本量，p 为自变量个数，RSS 为残差平方和，TSS 为总离差平方和。

**加入一个纯随机无关特征后：**

（1）**RSS 的变化：** OLS 在扩大的特征空间中总能找到不劣于原模型的拟合，即使新特征与 Y 完全无关，也会利用该特征的随机波动去拟合一部分噪声，因此 RSS 不增（通常略降）。

（2）**自由度的变化：** 加入一个新变量后，自变量个数从 p 变为 p+1，残差自由度从 n-p-1 变为 n-p-2，即分母缩小。

**对 R² 的影响：**

由于 TSS 不变、RSS 不增（通常略降），R² = 1 - RSS/TSS 上升或保持不变。新特征即使完全无关，也不会让 R² 下降。

**对 R²_adj 的影响：**

R²_adj 在分子分母上同时扣除了自由度惩罚。新特征使残差自由度减少 1，当新特征对 RSS 的降低幅度不足以补偿自由度损失时：

RSS_新 / (n-p-2) > RSS_旧 / (n-p-1)

从而 R²_adj 下降。纯随机特征对 RSS 的降低仅来自偶然噪声，平均而言不足以补偿 1 个自由度的代价，因此 R²_adj 通常会下降。

**结论：** R² 只看拟合优度、不惩罚变量数量；R²_adj 通过自由度惩罚，淘汰对 RSS 改善甚微的无用变量，因此加入无关特征时二者走向不同。

